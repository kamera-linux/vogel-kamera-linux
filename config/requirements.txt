# Vogel-Kamera-Linux Requirements - v1.3.0 (Trixie)
# Python dependencies for the CLIENT-PC

# NOTE: On Raspberry Pi with Trixie (Debian 13), use apt instead of pip!
# See: docs/INSTALLATION-TRIXIE.md
# sudo apt-get install python3-scp python3-paramiko

# SSH and file transfer
paramiko>=3.0.0
scp>=0.14.0

# Progress bars and user interface
tqdm>=4.60.0

# Configuration management  
python-dotenv>=1.0.0

# QR Code generation (optional, for YouTube integration)
qrcode[pil]>=7.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0