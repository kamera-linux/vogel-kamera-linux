all:
  hosts:
    pi-camera:
      ansible_python_interpreter: /usr/bin/python3
      # ansible_host, ansible_user, ansible_ssh_private_key_file
      # werden aus ansible/.env via group_vars geladen (lookup 'env')
